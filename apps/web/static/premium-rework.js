/* Structural product composition. API calls and actions stay in app.js. */
function productHeading(kicker,title,description){return `<div class="product-heading"><span class="section-kicker">${kicker}</span><h2>${title}</h2><p>${description}</p></div>`;}
renderHome=async function(){const home=await api(`/api/organizations/${state.org}/home`),messages=await api('/api/chat');$('#view').innerHTML=`<div class="home-stage"><div class="home-intro">${productHeading('TODAY',esc(home.organization_name||'Ваше пространство'),'Ваше рабочее пространство для решений, команды и проверенных результатов.')}<span class="status-pill"><i class="presence-dot"></i> В работе</span></div><div class="home-focus"><div class="focus-copy"><span class="section-kicker">ТЕКУЩИЙ ФОКУС</span><h2>${esc(home.goal_title||'Сформулируйте первую цель')}</h2><p>${esc(home.goal_state||'Iris поможет превратить замысел в понятный план и реальные результаты.')}</p><div class="focus-meta"><span>${home.team_size||0} участников</span><span>${Number(home.goal_progress||0)}% прогресса</span></div></div><div class="focus-action"><button class="primary" id="open-iris">Поговорить с Iris <b>→</b></button></div></div>${irisBlock(messages,true)}<div class="home-summary"><div><span>Команда</span><strong>${home.team_size||0}</strong><small>активных участников</small></div><div><span>Цель</span><strong>${Number(home.goal_progress||0)}%</strong><small>текущий прогресс</small></div><div><span>Следующий шаг</span><strong>${esc(home.goal_title||'Новая цель')}</strong><small>Iris ждёт решения</small></div></div></div>`;$('#open-iris').onclick=()=>setView('iris');$('#quick-composer').onsubmit=submitQuickMessage;};
renderIris=async function(){const messages=await api('/api/chat');$('#view').innerHTML=`<div class="iris-stage-page">${productHeading('IRIS · AI SUPERVISOR','Ваш рабочий центр','Сформулируйте результат естественным языком. Iris сохранит контекст и подключит нужную команду.')}${irisBlock(messages,false)}</div>`;$('#composer').onsubmit=submitMessage;};
renderTeam=async function(){const employees=await api(`/api/organizations/${state.org}/employees`);if(!employees.length)return emptyView('Команда ещё не собрана','Попросите Iris предложить состав команды под вашу цель.');$('#view').innerHTML=`<div class="team-stage">${productHeading('TEAM','Люди, которые двигают работу','Каждый участник видит свою часть задачи, роль и текущий вклад в общий результат.')}<div class="team-toolbar"><span>${employees.length} участников</span><button class="quiet-button" id="team-iris">Собрать команду через Iris</button></div><div class="team-list">${employees.map(employee=>`<article class="team-member"><div class="person-avatar">${esc((employee.display_name||'С').slice(0,1))}</div><div class="member-main"><h3>${esc(employee.display_name)}</h3><p>${esc(employee.primary_role||'Сотрудник')}</p></div><span class="member-state"><i class="presence-dot"></i>${esc(employee.lifecycle_state||'ACTIVE')}</span><button class="member-open" aria-label="Открыть профиль">→</button></article>`).join('')}</div></div>`;$('#team-iris').onclick=()=>setView('iris');};
renderWork=async function(){const work=await api('/api/work'),goals=await api('/api/goals');const progress=Math.min(100,Math.max(0,Number(work.goal_progress||0)));$('#view').innerHTML=`<div class="work-stage">${productHeading('WORK','Работа в движении','От цели до проверенного результата. Iris показывает, что происходит сейчас и что будет дальше.')}<div class="work-focus"><div><span class="section-kicker">АКТИВНАЯ ЦЕЛЬ</span><h2>${esc(work.goal_title||'Нет активной цели')}</h2><p>${esc(work.goal_state||'Готово к старту')}</p></div><strong>${progress}%</strong><div class="progress"><i style="width:${progress}%"></i></div></div><div class="work-flow"><div class="flow-label"><span class="section-kicker">ИСТОРИЯ ЦЕЛЕЙ</span><span>${goals.length} всего</span></div>${goals.length?goals.slice().reverse().map(goal=>`<article class="goal-row"><span class="flow-marker"></span><div><h3>${esc(goal.goal||goal.objective||'Цель')}</h3><p>${esc(goal.status||'Создано')}</p></div><button class="quiet-button" data-start-goal="${esc(goal.plan_id)}">Запустить</button></article>`).join(''):'<div class="state-panel compact-state"><p>Целей пока нет. Опишите Iris нужный результат.</p></div>'}</div></div>`;document.querySelectorAll('[data-start-goal]').forEach(button=>button.onclick=async()=>{button.disabled=true;try{await api(`/api/goals/${encodeURIComponent(button.dataset.startGoal)}/start`,{method:'POST'});await renderWork();}catch(error){button.disabled=false;errorView(error);}});};
renderFiles=async function(){const files=await api('/api/files');if(!files.length)return emptyView('Артефактов пока нет','Завершённая работа появится здесь вместе с источником и статусом проверки.');const base=API_BASE;$('#view').innerHTML=`<div class="files-stage">${productHeading('FILES','Библиотека результатов','Все созданные артефакты, источники и проверки в одном спокойном рабочем пространстве.')}<div class="file-library-head"><span>${files.length} артефактов</span><span>Последние обновления сверху</span></div><div class="file-list">${files.map(file=>`<article class="file-row"><span class="file-icon">↗</span><div><h3>${esc(file.title)}</h3><p>${esc(file.artifact_type||'Артефакт')}</p></div><span class="status-pill">${esc(file.status||'готов')}</span>${file.artifact_id?`<a class="quiet-button" href="${base}/api/files/${encodeURIComponent(file.artifact_id)}/preview" target="_blank" rel="noreferrer">Открыть</a>`:''}</article>`).join('')}</div></div>`;};
setTimeout(()=>{if(typeof state!=='undefined'&&typeof render==='function')render();},0);
