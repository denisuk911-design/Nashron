(function () {
  const apiBase = window.LUMINIFERA_API_BASE || "";
  const gate = document.querySelector("#auth-gate");
  if (!gate) return;
  const $ = id => document.querySelector(id);
  const copy = {
    ru: { eyebrow:"LUMINIFERA · IRIS", loginTitle:"Войдите в своё рабочее пространство", loginSub:"Ваши команды, цели и проверенные результаты сохранены в профиле.", bootstrapTitle:"Откройте своё рабочее пространство", bootstrapSub:"Создайте первый профиль владельца. Iris встретит вас внутри и поможет начать.", registerTitle:"Создайте профиль Luminifera", registerSub:"После регистрации Iris встретит вас и поможет начать работу.", passwordTitle:"Обновите пароль профиля", passwordSub:"Новый пароль защитит доступ к вашему рабочему пространству.", account:"Email или идентификатор профиля", name:"Ваше имя", password:"Пароль", language:"Язык интерфейса", login:"Войти", bootstrap:"Создать рабочее пространство", register:"Создать аккаунт", change:"Сохранить пароль", create:"Создать аккаунт", back:"Уже есть профиль? Войти", logout:"Выйти", statusCreated:"Профиль создан. Войдите, чтобы продолжить.", statusChanged:"Пароль изменён. Войдите снова.", invalidEmail:"Введите корректный email или идентификатор профиля.", nameRequired:"Укажите имя.", shortPassword:"Пароль должен содержать не менее 10 символов.", passwordRules:"Пароль должен содержать буквы и цифры.", disabled:"Регистрация сейчас недоступна. Для первого запуска используйте создание рабочего пространства.", duplicate:"Профиль с таким email уже существует.", invalid:"Профиль не найден или введены неверные данные.", rate:"Слишком много попыток. Повторите позже.", closed:"Первый профиль уже создан. Войдите с его данными.", network:"Не удалось связаться с Luminifera. Проверьте подключение и повторите попытку.", generic:"Не удалось выполнить действие. Проверьте данные и повторите попытку." },
    uk: { eyebrow:"LUMINIFERA · IRIS", loginTitle:"Увійдіть у свій робочий простір", loginSub:"Ваші команди, цілі та перевірені результати збережені у профілі.", bootstrapTitle:"Відкрийте свій робочий простір", bootstrapSub:"Створіть перший профіль власника. Iris зустріне вас усередині та допоможе почати.", registerTitle:"Створіть профіль Luminifera", registerSub:"Після реєстрації Iris зустріне вас і допоможе розпочати роботу.", passwordTitle:"Оновіть пароль профілю", passwordSub:"Новий пароль захистить доступ до вашого робочого простору.", account:"Email або ідентифікатор профілю", name:"Ваше ім'я", password:"Пароль", language:"Мова інтерфейсу", login:"Увійти", bootstrap:"Створити робочий простір", register:"Створити акаунт", change:"Зберегти пароль", create:"Створити акаунт", back:"Вже маєте профіль? Увійти", logout:"Вийти", statusCreated:"Профіль створено. Увійдіть, щоб продовжити.", statusChanged:"Пароль змінено. Увійдіть знову.", invalidEmail:"Введіть коректний email або ідентифікатор профілю.", nameRequired:"Вкажіть ім'я.", shortPassword:"Пароль має містити щонайменше 10 символів.", passwordRules:"Пароль має містити літери та цифри.", disabled:"Реєстрація зараз недоступна. Для першого запуску створіть робочий простір.", duplicate:"Профіль із таким email уже існує.", invalid:"Профіль не знайдено або введено невірні дані.", rate:"Забагато спроб. Повторіть пізніше.", closed:"Перший профіль уже створено. Увійдіть із його даними.", network:"Не вдалося зв'язатися з Luminifera. Перевірте підключення та повторіть спробу.", generic:"Не вдалося виконати дію. Перевірте дані та повторіть спробу." },
    en: { eyebrow:"LUMINIFERA · IRIS", loginTitle:"Enter your workspace", loginSub:"Your teams, goals, and verified results are saved in your profile.", bootstrapTitle:"Open your workspace", bootstrapSub:"Create the first owner profile. Iris will meet you inside and help you begin.", registerTitle:"Create your Luminifera profile", registerSub:"After registration, Iris will meet you and help you get started.", passwordTitle:"Update your profile password", passwordSub:"A new password protects access to your workspace.", account:"Email or profile identifier", name:"Your name", password:"Password", language:"Interface language", login:"Sign in", bootstrap:"Create workspace", register:"Create account", change:"Save password", create:"Create account", back:"Already have a profile? Sign in", logout:"Sign out", statusCreated:"Profile created. Sign in to continue.", statusChanged:"Password changed. Sign in again.", invalidEmail:"Enter a valid email or profile identifier.", nameRequired:"Enter your name.", shortPassword:"Password must contain at least 10 characters.", passwordRules:"Password must contain letters and numbers.", disabled:"Registration is currently unavailable. Use workspace creation for the first launch.", duplicate:"A profile with this email already exists.", invalid:"Profile not found or the credentials are incorrect.", rate:"Too many attempts. Try again later.", closed:"The first profile already exists. Sign in with its credentials.", network:"Luminifera could not be reached. Check your connection and try again.", generic:"The action could not be completed. Check the details and try again." }
  };
  let mode = "login", initialized = false, language = localStorage.getItem("luminifera.authLanguage") || document.documentElement.lang || "ru";
  const text = key => (copy[language] || copy.ru)[key] || copy.ru[key] || key;
  async function request(path, options = {}) {
    try {
      const requestOptions = { ...options, headers: { "Content-Type": "application/json", ...(options.headers || {}) } };
      if (requestOptions.body && typeof requestOptions.body !== "string") requestOptions.body = JSON.stringify(requestOptions.body);
      const response = await fetch(`${apiBase}${path}`, requestOptions);
      const data = await response.json().catch(() => ({}));
      if (!response.ok) { const error = new Error(typeof data.detail === "string" ? data.detail : `HTTP ${response.status}`); error.status = response.status; throw error; }
      return data;
    } catch (error) { if (error instanceof TypeError || error.name === "AbortError") throw new Error("network_unavailable"); throw error; }
  }
  function errorText(error) { return ({ registration_disabled:text("disabled"), invalid_credentials_or_blocked_account:text("invalid"), login_rate_limited:text("rate"), owner_bootstrap_closed:text("closed"), account_already_exists:text("duplicate"), invalid_account_id:text("invalidEmail"), invalid_display_name:text("nameRequired"), password_too_short:text("shortPassword"), password_requires_letters_and_digits:text("passwordRules"), network_unavailable:text("network") })[error.message] || text("generic"); }
  function setMode(next) {
    mode = next; const bootstrap = mode === "bootstrap", register = mode === "register", change = mode === "password";
    $("#auth-title").textContent = text(bootstrap ? "bootstrapTitle" : register ? "registerTitle" : change ? "passwordTitle" : "loginTitle");
    $("#auth-subtitle").textContent = text(bootstrap ? "bootstrapSub" : register ? "registerSub" : change ? "passwordSub" : "loginSub");
    $("#auth-name-label").hidden = !(bootstrap || register); $("#auth-account-label").hidden = change; $("#auth-account").required = !change; $("#auth-language-label").hidden = false; $("#auth-language").value = language;
    $("#auth-submit").textContent = text(bootstrap ? "bootstrap" : register ? "register" : change ? "change" : "login"); $("#auth-alt").hidden = change; $("#auth-alt").textContent = mode === "login" ? text("create") : text("back"); $("#auth-password").autocomplete = change ? "new-password" : "current-password"; $("#auth-error").hidden = true;
  }
  function applyLanguage() { document.documentElement.lang = language; localStorage.setItem("luminifera.authLanguage", language); $(".auth-copy .eyebrow").textContent = text("eyebrow"); $("#auth-account-label").childNodes[0].textContent = text("account"); $("#auth-name-label").childNodes[0].textContent = text("name"); $("#auth-password-label").childNodes[0].textContent = text("password"); $("#auth-language-label").childNodes[0].textContent = text("language"); $("#auth-logout").textContent = text("logout"); setMode(mode); }
  function ready() { gate.classList.add("auth-ready"); document.body.classList.remove("auth-gate-pending"); }
  function showAccount() { gate.classList.remove("auth-ready"); $("#auth-logout").hidden = false; $("#auth-password-change").hidden = false; setMode("password"); }
  async function open() { const token = localStorage.getItem("luminifera.authToken"); if (token) { try { await request("/api/auth/me", { headers:{Authorization:`Bearer ${token}`} }); initialized=true; ready(); return; } catch (_) { localStorage.removeItem("luminifera.authToken"); } } try { const status = await request("/api/auth/bootstrap-status"); setMode(status.owner_bootstrap === "available for fresh install" ? "bootstrap" : "login"); } catch (_) { setMode("login"); } initialized = true; applyLanguage(); }
  $("#auth-language").onchange = event => { language = event.target.value; applyLanguage(); }; $("#auth-alt").onclick = () => { if (initialized) setMode(mode === "login" ? "register" : "login"); }; $("#auth-password-change").onclick = () => { if (initialized) setMode("password"); }; $("#profile-button")?.addEventListener("click", event => { event.preventDefault(); showAccount(); });
  $("#auth-logout").onclick = async () => { try { await request("/api/auth/logout", { method:"POST", headers:{Authorization:`Bearer ${localStorage.getItem("luminifera.authToken")}`} }); } catch (_) {} localStorage.removeItem("luminifera.authToken"); $("#auth-logout").hidden=true; $("#auth-password-change").hidden=true; setMode("login"); $("#auth-status").textContent = text("login"); };
  $("#auth-form").onsubmit = async event => { event.preventDefault(); if (!initialized) return; const error=$("#auth-error"); error.hidden=true; const payload={account_id:$("#auth-account").value.trim(),password:$("#auth-password").value}; if (mode === "bootstrap" || mode === "register") { payload.display_name=$("#auth-name").value.trim(); payload.language=language; } try { let result; if (mode === "bootstrap") { await request("/api/auth/bootstrap", {method:"POST",body:payload}); result=await request("/api/auth/login", {method:"POST",body:{account_id:payload.account_id,password:payload.password}}); } else if (mode === "register") { await request("/api/auth/register", {method:"POST",body:payload}); setMode("login"); $("#auth-account").value=payload.account_id; $("#auth-status").textContent=text("statusCreated"); return; } else if (mode === "password") { await request("/api/auth/password", {method:"PUT",headers:{Authorization:`Bearer ${localStorage.getItem("luminifera.authToken")}`},body:{password:payload.password}}); localStorage.removeItem("luminifera.authToken"); setMode("login"); $("#auth-status").textContent=text("statusChanged"); return; } else result=await request("/api/auth/login", {method:"POST",body:payload}); if (result.token) { localStorage.setItem("luminifera.authToken",result.token); await request("/api/settings", {method:"PATCH",body:{interface_language:language}}).catch(()=>{}); ready(); } } catch (err) { error.textContent=errorText(err); error.hidden=false; } };
  applyLanguage(); open();
})();
