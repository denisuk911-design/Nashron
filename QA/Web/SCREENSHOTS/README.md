# Web visual evidence

The supplied reference material is stored outside the repository under `Инструкции/Web` and was used as the visual source of truth. Automated HTTP/API smoke is recorded in `QA/Web/API_TEST_REPORT.md`. Browser screenshot capture remains a follow-up because the current environment has no installed browser automation runtime; no screenshot is presented as visual acceptance.
