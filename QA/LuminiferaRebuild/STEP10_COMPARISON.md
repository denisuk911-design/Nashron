# STEP 10 comparison

The owner profile preview was opened and captured in `Screenshots/09_profile.png`. The view exposes an owner display name and owner avatar selection without mixing employee identity data into the profile workflow. Values are returned to the existing settings persistence path.
