# STEP 04 visual check

The packaged screenshot `03_iris.png` confirms a dedicated owner surface with Iris branding, a persistent message list, a natural-language input, and explicit confirmation support.

- Technical `Supervisor` and `Team2050` labels are absent from the visible owner surface.
- The action boundary remains the existing application service.
- Confirmation is a separate visible action and is not silently bypassed.
