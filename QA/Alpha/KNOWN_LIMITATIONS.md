# Alpha Known Limitations

- This checkpoint is a closed-alpha Web baseline, not a production installer.
- Provider-backed execution requires a configured provider and permissions in the selected organization.
- The legacy PySide client remains available as a fallback/test harness and is intentionally not replaced by this Web shell.
- Artifact preview is served through the existing scoped API preview endpoint.
